
  # Frontend Development Requirements

  This is a code bundle for Frontend Development Requirements. The original project is available at https://www.figma.com/design/Nn12AegCvrPXq91zMDiDbp/Frontend-Development-Requirements.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  